{
    "live-restore": true,
    {{$registries := getv "/env/nsecure-registries"}}
    {{if ne $registries ""}}
    "insecure-registries": ["{{ $insecure-registries }}"],
    {{end}}
    "ipv6": false
}
