{
    "live-restore": true,
    {{$registries := getv "/env/insecure-registries"}}
    {{if ne $registries ""}}
    "insecure-registries": {{split $registries " "|toJson}},
    {{end}}
    "ipv6": false
}